services:
  api:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "5000:8080"
    environment:
      - DOTNET_ENVIRONMENT=Development
    env_file:
      - server/API/.env